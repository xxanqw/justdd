[project]
name = "justdd"
version = "0.1.0"
description = "A simple, open-source tool to write Linux and Windows ISO images to USB drives."
readme = "README.md"
requires-python = ">=3.9, <3.14"
dependencies = [
    "PySide6"
]

[project.optional-dependencies]
dev = [
    "nuitka"
]
